import 'package:flutter/material.dart';

class ExperienceDetailPage extends StatelessWidget {
  const ExperienceDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text("Experiance page"),
    ));
  }
}